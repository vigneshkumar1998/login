

    console.log(typeof str);
    var str= new string("mom")
    function palindrome(str)
    {
        const len= string.length;
        for(i=0;i<len/2;i++)
        {
            if(string[i]!= string[len-1-i])
            return(" string is not palindrome");
            else
            return ("string is palindrime");
        }

    }
